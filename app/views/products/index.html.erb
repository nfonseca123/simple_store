<% @products.each do |product| %>
  <div class="product">
    <h2><%= product.title %></h2>
    <p><%= product.description %></p>
    <p>Price: $<%= product.price %></p>
    <p>Category: <%= product.category.name %></p>
    <a href="<%= product_path(product) %>">View Details</a>
  </div>
<% end %>
